<%= form_with(model: book, local: true) do |form| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :year %>
    <%= form.number_field :year, id: :book_year %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :book_title %>
  </div>

  <div class="field">
    <%= form.label :ISBN %>
    <%= form.number_field :ISBN, id: :book_ISBN %>
  </div>

  <div class="field">
    <%= form.label :author_id %>
    <%= collection_select( :book ,:author_id,Author.all,:id, :full_name,{:multple=> false})%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
